# Application
APP_NAME=hot-and-cold-memory
DEBUG=true
LOG_LEVEL=INFO

# API
API_HOST=0.0.0.0
API_PORT=8000
API_WORKERS=1

# Vector Database (Qdrant)
VECTOR_DB_HOST=localhost
VECTOR_DB_PORT=6333
VECTOR_DB_COLLECTION=hot_and_cold_memory

# Metadata Database (PostgreSQL)
METADATA_DB_URL=postgresql+asyncpg://memory:memory_password@localhost:5432/hot_and_cold_memory

# Cache (Redis)
CACHE_URL=redis://localhost:6379/0

# Document Store
DOCUMENT_STORE_PATH=./data/documents

# Embedding (choose one)
# Option 1: OpenAI (paid, cloud)
EMBEDDING_PROVIDER=openai
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_DIMENSION=1536

# Option 2: sentence-transformers (free, local)
# EMBEDDING_PROVIDER=sentence-transformers
# LOCAL_EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2   # 384d, fast
# LOCAL_EMBEDDING_MODEL=sentence-transformers/all-mpnet-base-v2  # 768d, better quality
# LOCAL_EMBEDDING_MODEL=BAAI/bge-large-zh-v1.5                    # 1024d, Chinese optimized
# EMBEDDING_DIMENSION=384
# LOCAL_EMBEDDING_DEVICE=cpu

# LLM (OpenAI-compatible - required for compression)
LLM_API_KEY=sk-your-openai-api-key
LLM_BASE_URL=https://api.openai.com/v1
COMPRESSION_MODEL=gpt-4o-mini
COMPRESSION_MAX_TOKENS=256
LLM_MAX_TOKENS=4096
LLM_TIMEOUT_SECONDS=60

# Tier Configuration
HOT_TO_COLD_THRESHOLD=0.25
COLD_TO_HOT_THRESHOLD=0.7
HOT_ACCESS_COUNT_THRESHOLD=50
COLD_TIER_COMPRESSION_RATIO=0.2

# Tier Capacity
HOT_TIER_CAPACITY=10000
HOT_TIER_EVICT_PERCENT=0.1

# Frequency Tracking
DECAY_HALF_LIFE_HOURS=72.0
QUERY_CLUSTERING_THRESHOLD=0.85

# Migration
MIGRATION_BATCH_SIZE=100
MIGRATION_INTERVAL_MINUTES=60
MIGRATION_MAX_CONCURRENT=5

# CORS (comma-separated origins; "*" for dev only)
CORS_ALLOW_ORIGINS=*
